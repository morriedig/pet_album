<p>this is index</p>

<% @pets.each do |pet| %>
  <p>
    <%= link_to "#{pet.name}", pet_path(pet) %>
    <%= image_tag pet.photo.thumb.url %>
    <%= pet.description %>
  </p>
<% end %>

<%= link_to "New", new_pet_path %>